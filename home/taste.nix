# Exported as `homeModules.taste`. The curated values are option defaults in
# each tool's module, so a single `dotfiles.profile.<tool>.enable` also works.
{
  dotfiles.profile = {
    ai.enable = true;
    k9s.enable = true;
    kitty.enable = true;
    zed.enable = true;
  };
}
